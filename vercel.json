{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack pnpm install --frozen-lockfile && OD_WEB_OUTPUT_MODE=server corepack pnpm --filter @open-design/web build",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
